<div class="dashboard-container">
      <%# flash and erorr %>
  <% if notice %>
    <p class="text-green-500 bg-green-100 border border-green-400 rounded p-2 mb-4"><%= notice %></p>
  <% end %>
  <% if alert %>
    <p class="text-red-500 bg-red-100 border border-red-400 rounded p-2 mb-4"><%= alert %></p>
  <% end %>

  <div class="dashboard-header">
    <h1 class="dashboard-title">Properties</h1>
    <%= link_to new_property_path, class: "add-property-btn" do %>
      <i class="fas fa-plus mr-2"></i>
      Add Property
    <% end %>
    
  </div>

  <div class="properties-grid">
    <% @properties.each do |property| %>
      <div class="property-card">
        <div class="property-header-list">
          <div>
            <h2 class="property-name bg-red"><%= property.name %></h2>
            <p class="property-address">
              <i class="fas fa-home"></i>
              <%= property.address %>
            </p>
          </div>
        </div>

        <div class="property-actions">
          <%= link_to property_jobs_path(property), class: "action-link" do %>
            View Jobs (<%= property.jobs.count %>)
          <% end %>
          
          <%= link_to property_checklists_path(property), class: "action-link" do %>
            Checklist
            <% if property.default_checklist.present? %>
              (<%= property.checklists.count %>)
            <% else %>
              (None)
            <% end %>
          <% end %>
                    <%= link_to property_reservations_path(property), class: "action-link" do %>
            View reservation (<%= property.reservations.count %>)
          <% end %>
          <%= link_to property_upsells_path(property), class: "action-link" do %>
            View upsell (<%= property.upsells.count %>)
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>