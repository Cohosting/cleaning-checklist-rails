<%= form_with model: [@organization, @property, @job] do |f| %>
  <div>
    <%= f.label :date, "Cleaning Date" %>
    <%= f.date_field :date %>
  </div>

 

  <div>
    <%= f.submit "Create Job", class: "btn btn-primary" %>
  </div>
<% end %>
